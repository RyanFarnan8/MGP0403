<form method="post" action="{{ path('my_job_applications_delete', {'id': my_job_application.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ my_job_application.id) }}">
    <button class="btn">Delete</button>
</form>
