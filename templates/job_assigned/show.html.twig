{% extends 'base.html.twig' %}

{% block title %}JobAssigned{% endblock %}

{% block body %}
    <h1>JobAssigned</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ job_assigned.id }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ job_assigned.contact }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ job_assigned.description }}</td>
            </tr>
            <tr>
                <th>Location</th>
                <td>{{ job_assigned.location }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ job_assigned.price }}</td>
            </tr>
            <tr>
                <th>Tradeperson</th>
                <td>{{ job_assigned.tradeperson }}</td>
            </tr>


        </tbody>
    </table>

{#    <a href="{{ path('job_assigned_index') }}">back to list</a>#}

{#    <a href="{{ path('job_assigned_edit', {'id': job_assigned.id}) }}">edit</a>#}

{#    {{ include('job_assigned/_delete_form.html.twig') }}#}
{% endblock %}
