{% extends 'base.html.twig' %}

{% block title %}Plumbing Page{% endblock %}

{% block body %}
<body>
<br>
<br>
{#    <form class="pull-right hidden" role="search" id="nav-search-form" method="post" action="{{ path('search') }}">#}
{#        <div class="input-group">#}
{#            <input type="text" class="form-control" placeholder="Search" name ="search">#}
{#            <span class="input-group-btn">#}
{#            <button type="submit" class="btn btn-default"></button>#}
{#          </span>#}
{#        </div>#}
{#    </form>#}



<form method="post" action="{{ url("search_county") }}">

    <input name="county">
    <input type="submit">
</form>
<br>
<br>




    <h3>Here is a list of Plumbing Jobs </h3>
    <table>
        <thead>
        <tr>

            <th>Creator</th>
            <th>Contact</th>
            <th>Trade</th>
            <th>Description</th>
            <th>County</th>
            <th>Location</th>
            <th>Timeslot</th>
        </tr>
        </thead>
        <tbody>{% for job in jobs%}
            {% if job.trade == 'Plumbing' %}
        <tr>
            <td>{{ job.creator}}</td>
            <td>{{ job.contact}}</td>
            <td>{{ job.trade }}</td>
            <td>{{ job.description }}</td>
            <td>{{ job.county }}</td>
            <td>{{ job.location }}</td>
            <td>{{ job.timeslot }}</td>
        </tr>



{% endif %}

        {% endfor %}</tbody>


    </table>

    <br>
    <br>
    <a href="{{ path('job_new') }}">
        <button type="button">Post A Job</button></a>

</body>


{% endblock %}
